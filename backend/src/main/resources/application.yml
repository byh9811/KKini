spring:
  profiles:
    active:
      - local
    group:
      local:
        - db-local
      dev:
        - db-dev
    include:
      - db
      - jwt
      - log
      - server
      - swagger
      - oauth2
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher